[gd_scene load_steps=5 format=3 uid="uid://level_01"]

[ext_resource type="Script" path="res://scripts/level_01.gd" id="1"]
[ext_resource type="Script" path="res://scripts/player.gd" id="2"]
[ext_resource type="Texture2D" path="res://assets/sprites/player.png" id="3"]

[sub_resource type="CapsuleShape2D" id="player_stand"]
radius = 7.0
height = 8.0

[sub_resource type="RectangleShape2D" id="player_crouch"]
size = Vector2(14.0, 10.0)

; ─── Level 01 — The Museum Lobby ─────────────────────────────────────────────
; Walls, guard and shadow zones are spawned at runtime by level_01.gd.
; Only the Player, Camera and HUD live here in the scene file.
; ──────────────────────────────────────────────────────────────────────────────

[node name="Level01" type="Node2D"]
script = ExtResource("1")

[node name="Player" type="CharacterBody2D" parent="." groups=["player"]]
position = Vector2(48.0, 288.0)
collision_layer = 2
collision_mask = 5
script = ExtResource("2")

[node name="Sprite2D" type="Sprite2D" parent="Player"]
texture = ExtResource("3")
scale = Vector2(2.0, 2.0)

[node name="CollisionShape2D" type="CollisionShape2D" parent="Player"]
shape = SubResource("player_stand")

[node name="CrouchCollision" type="CollisionShape2D" parent="Player"]
shape = SubResource("player_crouch")
disabled = true

[node name="Camera2D" type="Camera2D" parent="Player"]
zoom = Vector2(2.5, 2.5)
position_smoothing_enabled = true
position_smoothing_speed = 8.0

[node name="HUD" type="CanvasLayer" parent="."]

[node name="LevelLabel" type="Label" parent="HUD"]
offset_left = 10.0
offset_top = 10.0
text = "Level 1 — The Museum Lobby"

[node name="HintLabel" type="Label" parent="HUD"]
offset_left = 10.0
offset_top = 30.0
text = "WASD: move   Shift: crouch   Ctrl: dash   Reach the EXIT"
add_theme_font_size_override("font_size") = 11
